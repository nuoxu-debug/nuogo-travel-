{
  "name": "nuogo",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "bootstrap:node": "powershell -ExecutionPolicy Bypass -File scripts/bootstrap-node.ps1",
    "dev": "concurrently -n API,WEB -c yellow,cyan \"npm --workspace server run dev\" \"npm --workspace client run dev\"",
    "build": "npm --workspace shared run test && npm --workspace server run build && npm --workspace client run build",
    "test": "npm --workspace shared run test && npm --workspace server run test && npm --workspace client run test",
    "test:e2e": "playwright test",
    "test:shared": "npm --workspace shared run test",
    "ingest:anhui": "npm --workspace server run ingest:anhui --",
    "attractions:list": "npm --workspace server run attractions:list --",
    "attractions:review": "npm --workspace server run attractions:review --"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "concurrently": "^9.0.1"
  }
}
